workflows:
  windows-workflow:
    name: Windows Build
    instance_type: windows_x2
    environment:
      flutter: stable
    scripts:
      - name: Install dependencies
        script: flutter pub get
      - name: Build Windows exe
        script: flutter build windows --release
    artifacts:
      - build/windows/runner/Release/echotrace.exe
